<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Envelope.ResourceDictionaries">
    <Style TargetType="HeaderedContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="HeaderedContentControl">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.35*"/>
                            <ColumnDefinition Width="0.65*"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Column="0" HorizontalAlignment="Right"  ContentSource="Header"/>
                        <ContentPresenter Grid.Column="1" ContentSource="Content"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Thickness x:Key="HeaderRightMargin">0,0,10,0</Thickness>

    

</ResourceDictionary>